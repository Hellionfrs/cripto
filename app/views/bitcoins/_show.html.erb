
    <% @bitcoins.each do |bitcoin| %>
    <div class="flex items-center justify-center py-1">
      <ul>
        <li>
          <div class="flex items-center ">
            <div class="appearance-none bg-white border border-rose-200 rounded w-full text-gray-700 mr-3  py-2 px-2 leading-tight focus:outline-none"> hash: <%= bitcoin.block_hash["hash"]%></div> 
            
            <%= button_tag "↓", 
            class:"text-white bg-red-700 hover:bg-red-800 focus:ring-4 focus:outline-none focus:ring-red-300 font-medium rounded-lg text-sm px-4 py-2 px-2 text-center  items-center dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-800",
            id:"button-#{bitcoin.id}",
            data: {
              controller: "info",
              action: "info#toggleForm",
              info_form_param: "edit-info-#{bitcoin.id}"
            }%>
            <%= link_to "Destroy", bitcoin_path(bitcoin), data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                    }, class:"flex-shrink-0 bg-rose-400 hover:bg-red-700 border-rose-400 hover:border-red-700 text-sm border-4 text-white py-1 px-2 rounded" %>
            <%# <%= link_to "Edit", edit_bitcoin_path(bitcoin), class:"flex-shrink-0 bg-rose-400 hover:bg-rose-700 border-rose-400 hover:border-rose-700 text-sm border-4 text-white py-1 px-2 rounded"%> 
          </div>
        </li>
        <!-- Dropdown menu -->
        <li>
          <div id="edit-info-<%= bitcoin.id %>" class="hidden appearance-none bg-white border border-rose-200 rounded w-full text-gray-700 mr-3  py-2 px-2 leading-tight focus:outline-none">
            <ul>
              <li> hash: <%= bitcoin.block_hash["hash"]%></li>
              <li> prev_block: <%= bitcoin.block_hash["prev_block"]%></li>
              <li> time:<%= bitcoin.block_hash["time"]%></li>
              <li> bits:<%= bitcoin.block_hash["bits"]%></li>
            </ul>
          </div>
        </li>
      </ul>
      
    </div>
    <% end %>
  <%= link_to "New Bitcoin", new_bitcoin_path %>